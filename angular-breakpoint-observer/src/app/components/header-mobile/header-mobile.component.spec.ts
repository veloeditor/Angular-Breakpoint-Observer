import { HeaderMobileComponent } from './header-mobile.component';

describe('HeaderMobileComponent', () => {
  let component: HeaderMobileComponent;

  beforeEach(() => {
    component = new HeaderMobileComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
